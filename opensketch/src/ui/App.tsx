/**
 * STUB - wird vom UI-Entwickler ersetzt.
 * Der Export `App` ist Contract (wird von `src/main.tsx` gemountet).
 */

export function App() {
  return (
    <div className="flex h-full w-full items-center justify-center bg-panel-900 text-panel-300">
      <div className="text-center">
        <div className="text-lg font-semibold text-panel-100">OpenSketch Studio</div>
        <div className="mt-1 text-sm">Oberflaeche wird gerade gebaut ...</div>
      </div>
    </div>
  )
}

export default App
